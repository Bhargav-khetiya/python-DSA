//each time you make changes:
git status         # Optional: check the status
git add .          # Stage all changes
git commit -m "Your commit message"  # Commit the changes
git push           # Push the changes to GitHub


//If you made any changes to the .gitignore file, stage and commit those changes:
git add .gitignore
git commit -m "Update .gitignore to exclude git command.txt"
